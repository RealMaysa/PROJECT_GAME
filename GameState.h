/*#ifndef GAMESTATE_H
#define GAMESTATE_H
#include"State.h"
class GameState:
public State
{
private:
  

public:
    GameState(sf::RenderWindow*window);
    ~GameState();
    //Function
   void endState();
    //void update();
    void render(sf::RenderTarget*target=nullptr);
};
#endif
*/